The tsconfig.json defines the outFile in order to name it when the compiler is executed.
Then, there's no need to create a package.json script "tsc index.js" because it is defined in tsconfig.json
It is only necessary "tsc" in the script.